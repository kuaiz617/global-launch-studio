export function journeyPage(data){return `<div class="grid">${data.stages.map((s,i)=>`<div class="card stage"><div class="row"><span class="pill">0${i+1}</span><h3>${s.name} · ${s.zhName}</h3></div><p>${s.objective}</p><div class="muted">Success signal: ${s.successSignal}</div><div class="muted" style="margin-top:5px">Primary agents: ${data.agents.filter(a=>a.stage===s.id).map(a=>a.name).join(', ')}</div></div>`).join('')}</div>`;}
