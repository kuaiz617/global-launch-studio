import { postJSON } from '../api.js';
export function contentPage(data){return `<div class="two"><div class="card"><h3>Content brief</h3><select id="content-seller">${data.sellers.map(s=>`<option value="${s.id}">${s.name}</option>`).join('')}</select><select id="format"><option value="email">Seller email</option><option value="faq">FAQ</option><option value="checklist">Checklist</option></select><select id="language"><option value="en">English</option><option value="zh">中文</option></select><button id="generate" class="primary">Generate content</button></div><div class="card"><h3>Generated asset</h3><div id="content-result" class="muted">Choose a format and generate a grounded seller-education asset.</div></div></div>`;}
export function bindContent(){const btn=document.querySelector('#generate');if(!btn)return;btn.onclick=async()=>{const r=await postJSON('/api/content',{sellerId:document.querySelector('#content-seller').value,format:document.querySelector('#format').value,language:document.querySelector('#language').value});document.querySelector('#content-result').innerHTML=`<h3>${r.title}</h3><div class="result">${r.body}</div>`;};}
