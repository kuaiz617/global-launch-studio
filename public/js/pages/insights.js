export function insightsPage(result){const m=result.metrics;const metrics=[['Routing accuracy',m.routingAccuracy],['Grounding rate',m.groundingRate],['Claim safety',m.claimSafetyRate],['CTA completeness',m.ctaCompleteness],['Average quality',m.averageQuality]];return `<div class="grid cards">${metrics.map(([label,val])=>`<div class="card"><span class="muted">${label}</span><strong style="font-size:25px;display:block;margin:8px 0">${val}%</strong><div class="bar"><span style="width:${val}%"></span></div></div>`).join('')}</div><div class="card" style="margin-top:16px"><h3>Labeled evaluation set</h3><table class="table"><thead><tr><th>Question</th><th>Expected</th><th>Actual</th><th>Quality</th></tr></thead><tbody>${result.rows.map(r=>`<tr><td>${r.question}</td><td>${r.expectedAgent}</td><td>${r.route.agentId}</td><td>${r.quality.score}%</td></tr>`).join('')}</tbody></table></div>`;}
