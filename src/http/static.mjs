import { readFile, stat } from "node:fs/promises";
import path from "node:path";
export function createStaticHandler(publicDir){ return async function serve(req,res){ const raw=decodeURIComponent(new URL(req.url,`http://${req.headers.host}`).pathname); const requestPath=raw==='/'?'/index.html':raw; const filePath=path.normalize(path.join(publicDir,requestPath)); if(!filePath.startsWith(publicDir)){res.writeHead(403);return res.end('Forbidden');} try{const details=await stat(filePath); if(!details.isFile()) throw new Error(); const data=await readFile(filePath); const ext=path.extname(filePath); const mime={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8'}; res.writeHead(200,{'Content-Type':mime[ext]||'application/octet-stream'});res.end(data);}catch{const index=await readFile(path.join(publicDir,'index.html'));res.writeHead(200,{'Content-Type':'text/html; charset=utf-8'});res.end(index);} }; }
