PORT=4173

# Default: fully local and reproducible.
LLM_MODE=deterministic
RAG_MODE=lexical

# Optional real OpenAI mode. Keep keys server-side; never expose them in browser code.
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-5-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
